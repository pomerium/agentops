apiVersion: v1
kind: Service
metadata:
  name: agentops
  namespace: agentops
  labels:
    app.kubernetes.io/name: agentops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agentops
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
