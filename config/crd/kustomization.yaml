apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Installs the AgentTemplate CRD. Referenced as a directory base by
# deploy/base so `kubectl apply -k` on an overlay also creates the CRD.
resources:
  - bases/agents.pomerium.com_agenttemplates.yaml
